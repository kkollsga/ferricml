/target
/benchmarks/alternatives/target/
# `make mutants` writes under dev-docs/temp; this catches a bare `cargo mutants`,
# which defaults its output to the workspace root.
/mutants.out/
.DS_Store
/dev-docs/
/inbox/
/.claude/
/.agents/
/AGENTS.md

# Byte cache from importing the gate scripts under scripts/
__pycache__/
*.pyc

# Local experiment inputs and generated artifacts
/research/
